export enum Role {
  DEFENDER = 'DEFENDER',
  ATTACKER = 'ATTACKER',
}

export enum ShipType {
  BATTLESHIP = 'battleship',
  CRUISER = 'cruiser',
  DESTROYER = 'destroyer',
  SUBMARINE = 'submarine',
}

export enum HitResult {
  HIT = 'hit',
  MISS = 'miss',
  SUNK = 'sunk',
}
